# Teams, but I wonder if these need to be changed in the future when
# we start adding identifiers to the robots.
from enum import Enum

# Teams
class Team(Enum):
    YELLOW = "yellow"
    BLUE = "blue"